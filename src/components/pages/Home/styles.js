const slider = {
    width: '100%',
    heigth: '500px',

}
const subslider = {
    marginTop: '70px',
    width: '100%',
    heigth: '500px',
}

const mainPage = {
    width: '100%',
}

const fl = {
    textDecoration: 'none',
    color: 'black',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
}

const styles = {
    slider,
    mainPage,
    subslider,
    fl,
}

export default styles;