const register = {
    width: '500px',
    padding: '5px',
}

const container = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
}

const styles = {
    register,
    container,
}
export default styles;